# QR Code Generator

## Overview
This Python script generates a QR code from a given text using the qrcode library. The generated QR code is displayed using matplotlib and can be saved as a JPG file.

## Installation
Install the qrcode library using the following command:
```bash
pip install qrcode
